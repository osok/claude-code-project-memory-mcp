# Claude Memory MCP - Database Configuration
# ==========================================
#
# This file configures the database containers only.
# The MCP server runs locally and uses ~/.config/claude-memory/config.toml
# for its configuration (API keys, connection settings, etc.)
#
# Copy this file to .env:
#   cp .env.example .env
#
# Then edit .env with your Neo4j password.

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Neo4j Password (required)
# Set a secure password for the Neo4j database.
# This same password must be set in ~/.config/claude-memory/config.toml
NEO4J_PASSWORD=changeme

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Qdrant log level (optional)
# QDRANT_LOG_LEVEL=INFO

# =============================================================================
# NOTES
# =============================================================================
#
# API keys and other sensitive settings are now stored in the global config:
#   ~/.config/claude-memory/config.toml
#
# To create the config file, run:
#   claude-memory-mcp init-config
#
# Then edit it to add your Voyage API key and set the Neo4j password to match
# the NEO4J_PASSWORD value set in this file.
#
# The MCP server is started with:
#   claude-memory-mcp --project-id your-project-name
